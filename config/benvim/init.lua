-- All of my configuration lives in the `benvim` Lua module tree. Settings,
-- plugins (including the plugin loader itself), keymaps, and all other config
-- is handled there, so there's nothing else to do from our nvim init except
-- let it do its thing :)
require("benvim.boot")
